import { AppContainer } from 'components/AppContainer';
import { TaskList } from 'features/Tasks';

function App() {
  return (
    <AppContainer>
      <TaskList />
    </AppContainer>
  );
}

export default App;
